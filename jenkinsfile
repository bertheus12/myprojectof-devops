pipeline {
    agent any
    tools { maven 'Maven 3.8.4' }
    stages {
        stage('Build') {
            steps {
                withMaven(maven: 'Maven 3.8.4') {
                    sh 'mvn clean install'
                }
            }
        }
    }
}
